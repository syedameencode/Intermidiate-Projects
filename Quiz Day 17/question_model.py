import data

class Question:
    def __init__(self, q_text, q_answer):
        self.text = q_text #to print this type print(new_question.text)
        self.answer = q_answer #to print this type print(new_question.answer)
